/// One-time bulk import tool for USDA FoodData Central data.
///
/// Usage:
///   LIBERADO_DATABASE_URL=... liberado-import --source usda --file FoodData_Central_foundation_food.json
///
/// Not yet implemented; this is a placeholder to reserve the crate in the workspace.
fn main() {
    eprintln!("liberado-import: USDA/OFF bulk import not yet implemented");
    std::process::exit(1);
}
